import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  heading: {
    textAlign: 'center',
    fontSize: 30,
    borderWidth: 1,
    borderRadius: 20,
    margin: 20,
  },
  iconStyle: {
    fontSize: 20,
    padding: 5,
  },
});
